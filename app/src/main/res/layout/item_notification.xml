<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardElevation="2dp"
    app:cardCornerRadius="2dp"
    android:layout_margin="2dp"
    app:cardBackgroundColor="@color/white"
    app:cardMaxElevation="2dp"
    app:cardPreventCornerOverlap="false"
    app:cardUseCompatPadding="false">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:orientation="vertical"
        android:background="@drawable/btn_transparent">

        <TextView android:id="@+id/tvNotiMessage"
            style="@style/DefaultTextView"
            android:drawableLeft="@drawable/ic_circle_16"
            android:drawablePadding="10dp"
            android:drawableTint="@color/primary_500"
            tools:text="New Product! Product Name Available Now. "/>

        <TextView android:id="@+id/tvNotiDateTime"
            style="@style/SmallTextView"
            tools:text="Oct 31, 2022 at 12:14 PM"
            android:paddingTop="10dp"
            android:layout_marginLeft="25dp"/>

        <TextView android:id="@+id/tvNotiType"
            style="@style/SmallTextView"
            tools:text="Noti Type"
            android:visibility="gone"
            tools:visibility="visible"/>

        <TextView android:id="@+id/tvNotiID"
            style="@style/SmallTextView"
            tools:text="ProductID or SaleOrderID"
            android:visibility="gone"
            tools:visibility="visible"/>

    </LinearLayout>

</androidx.cardview.widget.CardView>

